# 🍞 Crust & Cream 베이커리 웹사이트 개발을 위한 5단계 액티비티 프롬프트

> **💡 바이브코딩 가이드:** 아래의 프롬프트를 순서대로 LLM(Cursor, Claude, v0 등)에 입력하여 빌드하세요. 각 단계가 성공적으로 완료된 것을 확인한 후 다음 단계로 넘어가야 에러를 최소화할 수 있습니다.

---

## [Activity 1: Backend Setup with BGM Data]
**역할:** 시니어 백엔드 엔지니어  
**목표:** 메뉴 설명, 가격, 오디오 정보가 포함된 백엔드 데이터베이스 구조와 베이스라인을 구축합니다.  

**요구사항:**
1. 프로젝트 폴더 구조를 MVC 패턴에 맞게 생성해줘.
2. `Menu` 테이블 스키마에 `id`, `name`, `description`, `price`, `image_url`, `bgm_title`, `bgm_artist`, `bgm_url` 필드를 포함해줘.
3. 다음 데이터를 DB 초기값(Seed Data)으로 자동 삽입되도록 스크립트를 짜줘. `bgm_url`은 테스트용 무료 mp3 링크나 플레이스홀더를 사용해줘.
   - **이름:** 소금빵 / **가격:** 3,200원 / **설명:** 겉은 바삭하고 속은 촉촉하며 버터의 풍미와 짭조름한 소금이 어우러진 시그니처 메뉴 / **음악:** 모차르트(Mozart) - 피아노 소나타 11번
   - **이름:** 조각 딸기케이크 / **가격:** 7,500원 / **설명:** 신선한 생딸기와 부드러운 마스카포네 크림이 겹겹이 샌드된 달콤한 케이크 / **음악:** 바흐(Bach) - 미뉴에트 G장조
   - **이름:** 단팥빵 / **가격:** 2,500원 / **설명:** 국산 팥으로 직접 끓여 달지 않고 담백한 앙금이 가득 찬 전통 단팥빵 / **음악:** 비발디(Vivaldi) - 사계 중 '봄'
4. 5000번 포트에서 구동되는 Express/FastAPI 서버를 만들고 `GET /api/menus` API를 완성해줘.

---

## [Activity 2: Authentication & Secure User API]
**역할:** 백엔드 보안 전문가  
**목표:** 로그인 및 회원 전용 예약 데이터 조회를 위한 JWT 인증 시스템을 만듭니다.  

**요구사항:**
1. `bcryptjs`를 사용하여 `POST /api/auth/register` 시 비밀번호를 안전하게 암호화해 저장해줘.
2. `POST /api/auth/login` 성공 시 유효기간 1일의 JWT 토큰을 발급하고 유저 정보와 함께 반환해줘.
3. 인증 미들웨어(`authMiddleware.js`)를 구현하여 토큰이 없는 요청은 401 에러를 반환하게 해줘.
4. 로그인한 유저의 예약 내역 조회를 위한 `GET /api/reservations/me` API 뼈대를 만들어줘.

---

## [Activity 3: Frontend Layout & Global Audio Player]
**역할:** 프론트엔드 UI/UX 아키텍트  
**목표:** 따뜻한 베이커리 감성의 UI 레이아웃과 배경음악을 제어할 수 있는 글로벌 오디오 컨트롤러를 만듭니다.  

**요구사항:**
1. Next.js App Router 또는 React Router를 사용해 홈(`/`), 로그인(`/login`), 예약(`/pickup`), 리뷰(`/reviews`) 라우팅을 설정해줘.
2. Tailwind CSS를 사용해 베이지와 크림 톤의 내비게이션 바를 만들고, 우측 상단에 현재 재생 중인 클래식 곡명 표시 및 [뮤트/재생]이 가능한 미니 오디오 플레이어 UI 컴포넌트를 공통 레이아웃에 포함해줘.

---

## [Activity 4: Menu Selection BGM Interaction & Pickup System]
**역할:** 풀스택 인터랙션 엔지니어  
**목표:** 사용자가 메뉴를 선택하면 백엔드 정보에 맞는 음악(모차르트, 바흐 등)이 재생되도록 프론트-백을 연결하고 예약 기능을 완성합니다.  

**요구사항:**
1. 메인 페이지에서 `GET /api/menus`를 호출해 빵 카드 리스트(사진, 이름, 설명, 가격 노출)를 렌더링해줘.
2. 사용자가 특정 빵 카드를 '클릭'하거나 '장바구니 담기'를 하면, 해당 메뉴의 `bgm_url`을 HTML5 `Audio` 객체로 넘겨받아 배경음악이 자동으로 부드럽게 전환되며 재생되도록 웹 오디오 로직을 짜줘. (예: 소금빵 클릭 시 모차르트 재생, 케이크 클릭 시 바흐 재생)
3. 예약 페이지(`/pickup`)에서 달력과 타임 슬롯을 선택해 장바구니에 담긴 데이터와 함께 `POST /api/reservations`로 예약을 전송하는 기능을 구현해줘.

---

## [Activity 5: Review Board & Audio UX Polish]
**역할:** QA 및 프론트엔드 피니셔  
**목표:** 리뷰 게시판을 완성하고, 브라우저의 오디오 자동재생 제한 정책(Autoplay Policy) 예외 처리를 적용합니다.  

**요구사항:**
1. 리뷰 페이지(`/reviews`)에서 리뷰 목록을 확인하고, 로그인한 유저만 별점과 텍스트를 입력해 리뷰를 등록(`POST /api/reviews`)할 수 있게 해줘.
2. 홈페이지 첫 진입 시 "음악과 함께 베이커리 둘러보기"와 같은 시작 버튼/모달을 띄워 브라우저 자동재생 제한 정책(Autoplay Policy)을 안전하게 우회하도록 UX를 개선해줘.
3. 비로그인 유저가 픽업 예약을 시도할 때 로그인 페이지로 리다이렉트하는 예외 처리를 완료해줘.